#include<bits/stdc++.h>
using namespace std;
void search(int arr[], int first, int last,int ele){
    if(first>last)
        return -1;
    int mid = (first+last)/2;
    if(arr[mid]==ele)
        return mid;
    if(arr[first]<=arr[mid]){
        
    }
}